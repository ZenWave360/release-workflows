name: Release Gradle project (reusable)

# Security model: see release-maven.yml's header -- same principle applies
# here. `environment:` and `secrets:` on jobs in this reusable workflow
# resolve against the CALLING repository, never against this shared repo.
# Callers pass secrets explicitly by name -- never `secrets: inherit`.
on:
  workflow_call:
    inputs:
      version:
        description: "Semantic version to release, without a v prefix (e.g. 1.0.0 or 1.0.0-rc.1)"
        required: true
        type: string
      development-version:
        description: "Next development version (e.g. 1.0.1-SNAPSHOT). Leave empty to auto-derive the next patch SNAPSHOT."
        required: false
        type: string
        default: ""
      group-id:
        description: "Maven groupId, e.g. io.zenwave360.jsonrefparser. Leave empty to derive it from build.gradle.kts's 'group = ...' line instead of duplicating it here."
        required: false
        type: string
        default: ""
      artifact-id:
        description: "Maven artifactId, e.g. json-schema-ref-parser-kmp"
        required: true
        type: string
      java-version:
        description: "JDK version for the build/prepare/publish steps"
        required: false
        type: string
        default: "17"
      java-distribution:
        description: "JDK distribution (setup-java 'distribution' input)"
        required: false
        type: string
        default: "temurin"
      node-version:
        description: "Node.js version for the Maven Central build job (Kotlin/JS toolchain)"
        required: false
        type: string
        default: "18"
      central-environment:
        description: "Name of the GitHub Environment (in the caller repo) gating Central publication"
        required: false
        type: string
        default: "maven-central-upload"
      develop-branch:
        description: "Branch to sync the release commit back into and to publish snapshots from"
        required: false
        type: string
        default: "develop"
      snapshot-workflow:
        description: "Filename (in the caller repo's .github/workflows/) of the snapshot-publish workflow to dispatch after sync"
        required: false
        type: string
        default: "publish-maven-snapshots.yml"
      publish-npm:
        description: "Whether to add a publishNpm dispatch input and wire up the publish-npm job. Set false for Gradle repos with no npm package."
        required: false
        type: boolean
        default: false
      npm-package:
        description: "npm package name including scope, e.g. @zenwave360/json-schema-ref-parser-kmp. Required if publish-npm is true."
        required: false
        type: string
        default: ""
      npm-package-dir:
        description: "Path to the Kotlin/JS-generated npm package directory relative to the repo root, e.g. build/js/packages/json-schema-ref-parser-kmp. Required if publish-npm is true."
        required: false
        type: string
        default: ""
      npm-environment:
        description: "Name of the GitHub Environment (in the caller repo) gating npm publication"
        required: false
        type: string
        default: "npm-publish"
      npm-node-version:
        description: "Node.js version for npm OIDC trusted publishing (needs npm >= 11.5.1, bundled from Node 24)"
        required: false
        type: string
        default: "24"
      dispatch-publish-npm:
        description: "The actual per-run value of the caller's publishNpm dispatch input (whether THIS run should publish to npm). Ignored if publish-npm (the capability flag) is false."
        required: false
        type: boolean
        default: false
    secrets:
      CENTRAL_USERNAME:
        required: true
      CENTRAL_TOKEN:
        required: true
      SIGN_KEY:
        required: true
      SIGN_KEY_PASS:
        required: true

# Default deny: every job gets read-only unless it explicitly declares more.
permissions:
  contents: read

jobs:
  # ---------------------------------------------------------------------------
  # validate: no secrets, read-only. See validate-release.yml.
  # ---------------------------------------------------------------------------
  validate:
    uses: ./.github/workflows/validate-release.yml
    with:
      version: ${{ inputs.version }}
      development-version: ${{ inputs.development-version }}
      group-id: ${{ inputs.group-id }}
      artifact-id: ${{ inputs.artifact-id }}
      publish-npm: ${{ inputs.publish-npm && inputs.dispatch-publish-npm }}
      npm-package: ${{ inputs.npm-package }}

  # ---------------------------------------------------------------------------
  # prepare-release: the ONLY job with contents:write/pull-requests:write
  # before publication. Bumps the version in build.gradle.kts with sed (NO
  # Gradle execution, so this job runs no repository-controlled code at all),
  # creates the release commit + next development commit on a throwaway
  # release/<version> branch, and lands them on main via a PR (merged
  # immediately, not pushed directly).
  #
  # Why sed instead of a Gradle task: unlike Maven, there is no standard
  # Gradle "release plugin" equivalent to maven-release-plugin that every
  # caller can rely on being present, so this mirrors what worked in this
  # org's Gradle repos already: a deterministic single-line replacement of
  # `version = "..."` in build.gradle.kts, validated by an exact allowlist
  # check (only that file may change) rather than trusting a plugin.
  #
  # Why a PR instead of a direct push: main's ruleset requires changes to go
  # through a pull request. A direct push would need a bypass actor added to
  # that ruleset -- but ruleset bypass is granted to an IDENTITY (e.g. "GitHub
  # Actions"), not to this one job. Any other workflow in the repo capable of
  # authenticating as that identity would inherit the same bypass, which is a
  # far bigger hole than this job's own blast radius. Merging a PR needs no
  # bypass at all: it is the standard, already-allowed path, so this design
  # adds no new privileged identity to the repository.
  #
  # Receives NO Central/npm/signing secrets; the GITHUB_TOKEN exists only in
  # the push/PR steps.
  # ---------------------------------------------------------------------------
  prepare-release:
    name: Create release commit and tag
    needs: validate
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: write # push the release branch and the tag
      pull-requests: write # open and merge the release PR
    outputs:
      release_commit_sha: ${{ steps.commit.outputs.release_commit_sha }}
    steps:
      - name: Checkout trusted main commit
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          ref: ${{ needs.validate.outputs.release_sha }}
          fetch-depth: 0
          persist-credentials: false

      - name: Refuse a stale release branch
        env:
          VERSION: ${{ needs.validate.outputs.version }}
        run: |
          set -euo pipefail
          if git ls-remote --exit-code --heads origin "release/${VERSION}" >/dev/null 2>&1; then
            echo "::error::Branch release/${VERSION} already exists on origin (leftover from a previous run?). Delete it and re-run."
            exit 1
          fi

      - name: Set release version and create release commit and tag
        id: commit
        env:
          VERSION: ${{ needs.validate.outputs.version }}
          TAG: ${{ needs.validate.outputs.tag }}
        run: |
          set -euo pipefail

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          set_version() { # set_version <new-version>
            if [[ "$(grep -c '^version = ' build.gradle.kts)" != "1" ]]; then
              echo "::error::Expected exactly one top-level 'version = ' line in build.gradle.kts."
              exit 1
            fi
            sed -i -E "s/^version = \".*\"$/version = \"$1\"/" build.gradle.kts
            if ! grep -qxF "version = \"$1\"" build.gradle.kts; then
              echo "::error::Failed to set version $1 in build.gradle.kts."
              exit 1
            fi
            # Only build.gradle.kts may change; anything else aborts the release.
            UNEXPECTED="$(git diff --name-only | grep -vxF 'build.gradle.kts' || true)"
            if [[ -n "${UNEXPECTED}" ]]; then
              echo "::error::Unexpected files modified by version update:"
              echo "${UNEXPECTED}"
              exit 1
            fi
            git --no-pager diff
          }

          set_version "${VERSION}"
          git commit -am "chore(release): release ${VERSION}"
          # Tag object is created locally now (pointing at this exact commit)
          # but only pushed once the PR below has actually merged into main.
          git tag -a "${TAG}" -m "Release ${VERSION}"
          echo "release_commit_sha=$(git rev-parse HEAD)" >> "${GITHUB_OUTPUT}"

      - name: Set next development version
        env:
          DEV_VERSION: ${{ needs.validate.outputs.dev_version }}
        run: |
          set -euo pipefail
          sed -i -E "s/^version = \".*\"$/version = \"${DEV_VERSION}\"/" build.gradle.kts
          if ! grep -qxF "version = \"${DEV_VERSION}\"" build.gradle.kts; then
            echo "::error::Failed to set next development version in build.gradle.kts."
            exit 1
          fi
          git --no-pager diff
          git commit -am "chore(release): prepare next development version ${DEV_VERSION}"

      # The token is only present in this step's env, never persisted to disk
      # or exposed to any build tool.
      - name: Push release branch
        env:
          GITHUB_TOKEN: ${{ github.token }}
          VERSION: ${{ needs.validate.outputs.version }}
        run: |
          set -euo pipefail

          ASKPASS="$(mktemp)"
          trap 'rm -f "${ASKPASS}"' EXIT
          printf '#!/bin/sh\nprintf %%s "${GITHUB_TOKEN}"\n' > "${ASKPASS}"
          chmod 700 "${ASKPASS}"

          GIT_ASKPASS="${ASKPASS}" git push \
            "https://x-access-token@github.com/${GITHUB_REPOSITORY}.git" \
            "HEAD:refs/heads/release/${VERSION}"

      - name: Open and merge release PR
        id: pr
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
          VERSION: ${{ needs.validate.outputs.version }}
          TAG: ${{ needs.validate.outputs.tag }}
        run: |
          set -euo pipefail
          PR_URL="$(gh pr create --base main --head "release/${VERSION}" \
            --title "chore(release): ${VERSION}" \
            --body "Automated release commit and next-development version bump for ${TAG}. This PR is required because main is protected; once merged, the release continues automatically.")"
          PR_NUMBER="${PR_URL##*/}"
          echo "pr_number=${PR_NUMBER}" >> "${GITHUB_OUTPUT}"
          # Merge immediately rather than requesting GitHub's native auto-merge
          # (which requires the repo-level "Allow auto-merge" setting and
          # would otherwise just fail with a GraphQL error). This works because
          # required approvals is 0 and main has no required status checks that
          # only run on pull_request events, so the PR is already mergeable.
          gh pr merge "${PR_NUMBER}" --merge --delete-branch

      # Safety net: the merge above is normally synchronous, but this confirms
      # it actually landed (e.g. if a merge queue or delayed check deferred
      # it) and fails fast on a real conflict or on the PR being closed
      # unmerged, instead of spinning for the full timeout.
      - name: Wait for release PR to merge
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
          PR_NUMBER: ${{ steps.pr.outputs.pr_number }}
        run: |
          set -euo pipefail
          ELAPSED=0
          TIMEOUT=900
          while true; do
            STATE="$(gh pr view "${PR_NUMBER}" --json state --jq '.state')"
            if [[ "${STATE}" == "MERGED" ]]; then
              break
            fi
            if [[ "${STATE}" == "CLOSED" ]]; then
              echo "::error::Release PR #${PR_NUMBER} was closed without merging."
              exit 1
            fi
            MERGEABLE="$(gh pr view "${PR_NUMBER}" --json mergeable --jq '.mergeable')"
            if [[ "${MERGEABLE}" == "CONFLICTING" ]]; then
              echo "::error::Release PR #${PR_NUMBER} conflicts with main (something else changed build.gradle.kts concurrently). Resolve manually and re-run the release."
              exit 1
            fi
            if (( ELAPSED >= TIMEOUT )); then
              echo "::error::Timed out waiting for release PR #${PR_NUMBER} to merge (state=${STATE}). Check required status checks / branch protection."
              exit 1
            fi
            sleep 15
            ELAPSED=$((ELAPSED + 15))
          done

      # Only pushed after the commit above is confirmed to be reachable from
      # main via the merge, so the tag can never point at unmerged content.
      - name: Push release tag
        env:
          GITHUB_TOKEN: ${{ github.token }}
          TAG: ${{ needs.validate.outputs.tag }}
        run: |
          set -euo pipefail
          ASKPASS="$(mktemp)"
          trap 'rm -f "${ASKPASS}"' EXIT
          printf '#!/bin/sh\nprintf %%s "${GITHUB_TOKEN}"\n' > "${ASKPASS}"
          chmod 700 "${ASKPASS}"

          GIT_ASKPASS="${ASKPASS}" git push \
            "https://x-access-token@github.com/${GITHUB_REPOSITORY}.git" \
            "refs/tags/${TAG}"

  # ---------------------------------------------------------------------------
  # build-and-publish-central: builds and uploads the Maven Central deployment
  # using the plugin's own publishToMavenCentral task (never
  # publishAndReleaseToMavenCentral, which would auto-release). Gated by the
  # caller repo's `central-environment` (default 'maven-central-upload':
  # required reviewer + deployments restricted to main) -- that approval
  # before this job even starts is the actual credential boundary. Central
  # tokens are not scoped per-operation, so a hand-rolled sign/zip/curl script
  # would not stop a stolen token any better than this does; it just added
  # complexity for no real security gain.
  #
  # This job is not secretless: `./gradlew build publishToMavenCentral` runs
  # the full build lifecycle (all configured plugins, all resolved
  # dependencies, the Kotlin/JS toolchain) with all four secrets present. The
  # "no build tool execution while secrets are present" goal holds only up to
  # the environment-approval gate, not inside this job.
  # ---------------------------------------------------------------------------
  build-and-publish-central:
    name: Build and publish Maven Central deployment (autoPublish=false)
    needs: [validate, prepare-release]
    runs-on: ubuntu-latest
    timeout-minutes: 60
    environment: ${{ inputs.central-environment }}
    permissions:
      contents: read
    steps:
      - name: Checkout immutable release commit
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          ref: ${{ needs.prepare-release.outputs.release_commit_sha }}
          persist-credentials: false

      # The committed gradle-wrapper.jar is executable code: verify its
      # checksum against Gradle's published checksums before running it.
      - name: Validate Gradle wrapper
        uses: gradle/actions/wrapper-validation@748248ddd2a24f49513d8f472f81c3a07d4d50e1 # v4.4.4

      - name: Set up JDK
        uses: actions/setup-java@c5195efecf7bdfc987ee8bae7a71cb8b11521c00 # v4.7.1
        with:
          java-version: ${{ inputs.java-version }}
          distribution: ${{ inputs.java-distribution }}
          cache: "gradle"

      - name: Setup Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: ${{ inputs.node-version }}

      - name: Build and publish to Maven Central (requires manual Publish in the Portal)
        run: |
          set -euo pipefail
          chmod +x gradlew
          ./gradlew build publishToMavenCentral
        env:
          ORG_GRADLE_PROJECT_mavenCentralUsername: ${{ secrets.CENTRAL_USERNAME }}
          ORG_GRADLE_PROJECT_mavenCentralPassword: ${{ secrets.CENTRAL_TOKEN }}
          ORG_GRADLE_PROJECT_signingInMemoryKey: ${{ secrets.SIGN_KEY }}
          ORG_GRADLE_PROJECT_signingInMemoryKeyPassword: ${{ secrets.SIGN_KEY_PASS }}

  # ---------------------------------------------------------------------------
  # create-github-release: runs AFTER the Central upload succeeded, so a GitHub
  # release is never announced for artifacts that failed to reach the Portal.
  # Deliberately does NOT wait for npm publication (which may be disabled or
  # approved later). Uses the committed notes from the immutable release commit.
  # ---------------------------------------------------------------------------
  create-github-release:
    name: Create GitHub release
    needs: [validate, prepare-release, build-and-publish-central]
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write # create the GitHub release for the existing tag
    steps:
      - name: Checkout immutable release commit
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          ref: ${{ needs.prepare-release.outputs.release_commit_sha }}
          persist-credentials: false

      - name: Create GitHub release from committed notes
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
          TAG: ${{ needs.validate.outputs.tag }}
          NOTES_FILE: ${{ needs.validate.outputs.notes_file }}
        run: |
          set -euo pipefail
          gh release create "${TAG}" --verify-tag \
            --title "${TAG}" \
            --notes-file "${NOTES_FILE}"

  # ---------------------------------------------------------------------------
  # publish-npm: the privileged npm job. Skipped entirely unless the caller
  # both enables npm capability (publish-npm: true) AND this specific run
  # requested it (dispatch-publish-npm: true). Gated by the caller repo's
  # `npm-environment` (default 'npm-publish'): its OWN required reviewer,
  # separate from Maven Central approval. No Maven Central credentials are
  # available here, and no npm credentials exist at all: publication uses
  # OIDC trusted publishing (id-token: write), which npm validates against
  # this repository + workflow + environment. Builds the package itself from
  # the immutable release commit (self-contained, no cross-job artifact) and
  # publishes with --ignore-scripts.
  # ---------------------------------------------------------------------------
  publish-npm:
    name: Publish npm package (OIDC trusted publishing)
    needs: [validate, prepare-release, build-and-publish-central]
    if: inputs.publish-npm && needs.validate.outputs.publish_npm == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    environment: ${{ inputs.npm-environment }}
    permissions:
      contents: read
      id-token: write # OIDC token for npm trusted publishing (no stored secret)
    steps:
      - name: Checkout immutable release commit
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          ref: ${{ needs.prepare-release.outputs.release_commit_sha }}
          persist-credentials: false

      - name: Validate Gradle wrapper
        uses: gradle/actions/wrapper-validation@748248ddd2a24f49513d8f472f81c3a07d4d50e1 # v4.4.4

      - name: Set up JDK
        uses: actions/setup-java@c5195efecf7bdfc987ee8bae7a71cb8b11521c00 # v4.7.1
        with:
          java-version: ${{ inputs.java-version }}
          distribution: ${{ inputs.java-distribution }}
          cache: "gradle"

      # npm >= 11.5.1 is required for OIDC trusted publishing; Node 24 bundles it.
      - name: Setup Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: ${{ inputs.npm-node-version }}
          registry-url: "https://registry.npmjs.org"

      - name: Build and pack npm package
        env:
          VERSION: ${{ needs.validate.outputs.version }}
          NPM_PACKAGE: ${{ inputs.npm-package }}
          PKG_DIR: ${{ inputs.npm-package-dir }}
        run: |
          set -euo pipefail
          if [[ -z "${NPM_PACKAGE}" || -z "${PKG_DIR}" ]]; then
            echo "::error::npm-package and npm-package-dir are required when publish-npm is true."
            exit 1
          fi
          chmod +x gradlew
          ./gradlew build

          if [[ ! -f "${PKG_DIR}/package.json" ]]; then
            echo "::error::Generated npm package not found at ${PKG_DIR}."
            exit 1
          fi
          NAME="$(jq -er '.name' "${PKG_DIR}/package.json")"
          PKG_VERSION="$(jq -er '.version' "${PKG_DIR}/package.json")"
          if [[ "${NAME}" != "${NPM_PACKAGE}" ]]; then
            echo "::error::package.json name '${NAME}' != expected '${NPM_PACKAGE}'."
            exit 1
          fi
          if [[ "${PKG_VERSION}" != "${VERSION}" ]]; then
            echo "::error::package.json version '${PKG_VERSION}' != release version '${VERSION}'."
            exit 1
          fi

          mkdir -p "${RUNNER_TEMP}/npm"
          npm pack --ignore-scripts --pack-destination "${RUNNER_TEMP}/npm" "./${PKG_DIR}"
          TARBALL="$(basename "$(find "${RUNNER_TEMP}/npm" -type f -name '*.tgz')")"

          # Tarball entries must all live under package/ (no traversal), and
          # the embedded package.json must match with no lifecycle scripts.
          if tar -tzf "${RUNNER_TEMP}/npm/${TARBALL}" | grep -Ev '^package/'; then
            echo "::error::Tarball contains entries outside package/."
            exit 1
          fi
          if ! tar -xzOf "${RUNNER_TEMP}/npm/${TARBALL}" package/package.json \
              | jq -e '(.scripts // {}) | length == 0' >/dev/null; then
            echo "::error::Tarball package.json declares lifecycle scripts; refusing to publish."
            exit 1
          fi

          echo "TARBALL=${TARBALL}" >> "${GITHUB_ENV}"

      - name: Publish to npm via OIDC trusted publishing
        env:
          VERSION: ${{ needs.validate.outputs.version }}
          NPM_PACKAGE: ${{ inputs.npm-package }}
        run: |
          set -euo pipefail

          NPM_VERSION="$(npm --version)"
          if [[ "$(printf '%s\n' 11.5.1 "${NPM_VERSION}")" != "$(printf '%s\n' 11.5.1 "${NPM_VERSION}" | sort -V)" ]]; then
            echo "::error::npm ${NPM_VERSION} < 11.5.1; OIDC trusted publishing unsupported."
            exit 1
          fi

          NPM_PACKAGE_URLENC="$(printf '%s' "${NPM_PACKAGE}" | sed 's#/#%2F#g')"

          # Duplicate guard immediately before publishing.
          META="$(curl -sS "https://registry.npmjs.org/${NPM_PACKAGE_URLENC}")"
          if ! jq -e 'has("versions")' <<< "${META}" >/dev/null; then
            echo "::error::${NPM_PACKAGE} not found on the registry; the first publish is manual."
            exit 1
          fi
          if ! jq --arg v "${VERSION}" -e '.versions[$v] | not' <<< "${META}" >/dev/null; then
            echo "::error::${NPM_PACKAGE}@${VERSION} is already published."
            exit 1
          fi

          # No token anywhere: npm exchanges the GitHub OIDC token for a
          # short-lived publish credential and attaches provenance
          # automatically. --ignore-scripts is defense in depth on top of the
          # scripts check above.
          npm publish "${RUNNER_TEMP}/npm/${TARBALL}" --ignore-scripts --access public

          {
            echo "### npm package published"
            echo "- \`${NPM_PACKAGE}@${VERSION}\` via OIDC trusted publishing (with provenance)"
            echo "- https://www.npmjs.com/package/${NPM_PACKAGE}/v/${VERSION}"
          } >> "${GITHUB_STEP_SUMMARY}"

  # ---------------------------------------------------------------------------
  # sync-develop: post-release housekeeping. Merges the release version bumps
  # from main back into the caller's develop branch and re-triggers snapshot
  # publication. Receives no publication or signing secrets; runs entirely
  # through the GitHub API.
  # ---------------------------------------------------------------------------
  sync-develop:
    name: Sync main back to ${{ inputs.develop-branch }}
    needs: [validate, create-github-release]
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: write # merge the sync PR
      pull-requests: write # create the sync PR
      actions: write # dispatch the snapshot-publish workflow
    steps:
      - name: Create and merge sync PR, then trigger snapshot publication
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
          TAG: ${{ needs.validate.outputs.tag }}
          DEVELOP_BRANCH: ${{ inputs.develop-branch }}
          SNAPSHOT_WORKFLOW: ${{ inputs.snapshot-workflow }}
        run: |
          set -euo pipefail

          AHEAD="$(gh api "repos/${GH_REPO}/compare/${DEVELOP_BRANCH}...main" --jq '.ahead_by')"
          if [[ "${AHEAD}" == "0" ]]; then
            echo "${DEVELOP_BRANCH} already contains main; nothing to sync."
          else
            PR_NUMBER="$(gh pr list --base "${DEVELOP_BRANCH}" --head main --state open --json number --jq '.[0].number // empty')"
            if [[ -z "${PR_NUMBER}" ]]; then
              PR_URL="$(gh pr create --base "${DEVELOP_BRANCH}" --head main \
                --title "Sync main back to ${DEVELOP_BRANCH} after ${TAG}" \
                --body "Syncs the ${TAG} release commit and next snapshot version back to ${DEVELOP_BRANCH}.")"
              PR_NUMBER="${PR_URL##*/}"
            fi
            gh pr merge "${PR_NUMBER}" --merge
          fi

          # Pushes made with GITHUB_TOKEN do not trigger workflows, so snapshot
          # publication for the new development version is dispatched explicitly.
          gh workflow run "${SNAPSHOT_WORKFLOW}" --ref "${DEVELOP_BRANCH}"
